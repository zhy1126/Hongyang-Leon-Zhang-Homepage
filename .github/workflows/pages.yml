name: Deploy static site to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Prepare site from visibility switch
        shell: bash
        run: |
          mkdir -p _site
          if grep -Eiq '^[[:space:]]*open[[:space:]]*$' site-status.txt; then
            rsync -a \
              --exclude '.git' \
              --exclude '.github' \
              --exclude '_site' \
              --exclude 'maintenance.html' \
              --exclude 'site-status.txt' \
              ./ _site/
          else
            mkdir -p _site/assets
            cp maintenance.html _site/index.html
            cp maintenance.html _site/404.html
            cp styles.css _site/styles.css
            cp assets/favicon.svg _site/assets/favicon.svg
            touch _site/.nojekyll
          fi

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5

      - name: Upload static site
        uses: actions/upload-pages-artifact@v4
        with:
          path: "_site"

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
